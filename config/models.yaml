allowed_codex_models:
  - gpt-5.5
  - gpt-5.4
  - gpt-5.3-codex
  - gpt-5.4-mini

notes:
  - Keep this list project-local.
  - The council never assigns a model outside this pool.
  - This default pool matches the model catalog exposed by Codex CLI 0.134.0 in this workspace.
